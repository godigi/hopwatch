# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Local logs and runtime artifacts
*.log
*.tmp

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Nimbalyst editor metadata (workspace-local, never committed)
nimbalyst-local/

# Local sandbox / scratch
scratch/
.cache/

# Python bytecode from helpers/
__pycache__/

# SwiftPM build products for gui/
gui/.build/
gui/build/
.swiftpm/
*.iconset/

# Claude Code personal project settings and local worktrees
.claude/

# Scratch state from the brainstorming skill's local preview server —
# a pid file, a stopped-server marker and generated HTML. Three of these
# were committed in 3e5ca0b before anyone noticed; a tracked .pid is
# stale the moment the process exits.
.superpowers/
gui/Resources/AppIcon.iconset
.Codex/
.nimbalyst/
